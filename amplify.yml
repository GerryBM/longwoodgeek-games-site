version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip install mkdocs-material
    build:
      commands:
        - mkdocs build
  artifacts:
    baseDirectory: site
    files:
      - '**/*'
  cache:
    paths: []
