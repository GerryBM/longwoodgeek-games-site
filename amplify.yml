version: 1
frontend:
  phases:
    preBuild:
      commands:
        - python --version
        - pip install mkdocs-material
    build:
      commands:
        - mkdocs build --strict
        - ls -la site
  artifacts:
    baseDirectory: site
    files:
      - '**/*'
  cache:
    paths: []
